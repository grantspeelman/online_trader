development:
  adapter: postgresql
  host: <%= ENV.fetch('DB_HOST') %>
  user: postgres
  database: online_trader_development

test:
  adapter: postgresql
  host: <%= ENV.fetch('DB_HOST') %>
  user: postgres
  database: online_trader_test
