<% if errors.any? %>
  <div class="alert alert-warning" role="alert">
    <h4 class="alert-heading"><%= pluralize(errors.count, "error") %>:</h4>
    <ul>
    <% errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>