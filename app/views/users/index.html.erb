<h1>Listing users</h1>

<%= paginate @users %>

<div class="listing">

  <% @users.each do |user| %>
      <div class="item user">
        <%= link_to(user.name, user) %>
        <div>
          IGN : <%= user.ign %>
          <br/>
          Wants : <%= user.wants.count %>
          <br/>
          Haves : <%= user.haves.count %>
        </div>
      </div>
  <% end %>

</div>

<br/>

<%= side_nav_link_to 'Add User', new_user_path if can? :create, User %>
