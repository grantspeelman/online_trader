<%= page_header('Editing want') %>

<%= render 'form' %>

<%= side_nav_link_to 'Show Want', @want %>
<%= side_nav_link_to "#{@want.user.name} Wants", user_wants_path(@want.user) if @want.user != current_user %>
